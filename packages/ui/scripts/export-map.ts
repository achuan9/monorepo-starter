import { COMPONENT_PREFIX } from '../../../scripts/constants';

export const exportMap = {
  [`${COMPONENT_PREFIX}Button`]: './components/button',
  [`${COMPONENT_PREFIX}Input`]: './components/input',
} as const;