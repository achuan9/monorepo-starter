# 贡献指南

感谢您对贡献本项目感兴趣！🎉

## 📋 目录

- [行为准则](#行为准则)
- [快速开始](#快速开始)
- [开发工作流](#开发工作流)
- [提交规范](#提交规范)
- [拉取请求流程](#拉取请求流程)
- [代码风格](#代码风格)
- [测试](#测试)

## 📜 行为准则

本项目遵循行为准则，所有贡献者都应遵守。请尊重和体谅他人。

## 🚀 快速开始

1. **Fork 仓库**
2. **克隆您的 Fork**

   ```bash
   git clone https://github.com/your-username/monorepo-starter.git
   cd monorepo-starter
   ```

3. **安装依赖**

   ```bash
   pnpm install
   ```

4. **创建分支**
   ```bash
   git checkout -b feat/your-feature-name
   ```

## 🔧 开发工作流

### 前置要求

- Node.js >= 24.0.0
- pnpm >= 9.0.0

### 可用脚本

- `pnpm dev` - 启动开发服务器
- `pnpm build` - 构建所有包
- `pnpm lint` - 检查所有包的代码
- `pnpm lint:fix` - 检查并自动修复问题
- `pnpm test` - 运行测试
- `pnpm format` - 格式化代码
- `pnpm check-types` - 类型检查所有包
- `pnpm spellcheck` - 检查拼写
- `pnpm spellcheck:fix` - 修复拼写问题

### 进行更改

1. 在功能分支中进行更改
2. 确保所有测试通过：`pnpm test`
3. 确保代码检查通过：`pnpm lint`
4. 确保类型正确：`pnpm check-types`
5. 格式化代码：`pnpm format`
6. 如需要，创建 changeset：`pnpm changeset`

## 📝 提交规范

我们遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范。

### 提交类型

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 仅文档变更
- `style`: 不影响代码含义的格式变更
- `refactor`: 既不修复 bug 也不添加功能的代码变更
- `perf`: 性能优化
- `test`: 添加缺失的测试或修正现有测试
- `chore`: 构建过程或辅助工具的变更
- `ci`: CI 配置文件和脚本的变更

### 提交格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 示例

```bash
feat(ui): add button component
fix(utils): resolve date formatting issue
docs: update README with new features
```

## 🔀 拉取请求流程

1. **更新您的分支**

   ```bash
   git checkout main
   git pull upstream main
   git checkout your-branch
   git rebase main
   ```

2. **确保所有检查通过**
   - 所有测试通过
   - 代码检查通过
   - 类型检查通过
   - 代码已格式化
   - 拼写检查通过

3. **创建拉取请求**
   - 使用清晰且描述性的标题
   - 填写 PR 模板
   - 链接相关 issue
   - 如适用，添加截图

4. **等待审查**
   - 处理任何审查意见
   - 保持您的 PR 与 main 分支同步

## 🎨 代码风格

### TypeScript/JavaScript

- 所有新代码使用 TypeScript
- 遵循 ESLint 规则
- 使用 Prettier 进行格式化
- 编写自文档化的代码，使用清晰的变量名

### Vue 组件

- 使用 Composition API
- 遵循 Vue 风格指南
- 组件 props 和 emits 使用 TypeScript

### 文件命名

- 组件：PascalCase（例如：`Button.vue`）
- 工具函数：camelCase（例如：`formatDate.ts`）
- 测试：`*.test.ts` 或 `*.spec.ts`

## 🧪 测试

- 为新功能编写测试
- 提交前确保所有测试通过
- 追求良好的测试覆盖率
- 使用描述性的测试名称

### 运行测试

```bash
# 运行所有测试
pnpm test

# 以监视模式运行测试
pnpm test --watch

# 运行测试并生成覆盖率报告
pnpm test --coverage
```

## 📦 Changesets

如果您的更改会影响将要发布的包：

1. 创建 changeset：`pnpm changeset`
2. 选择受影响的包
3. 选择版本更新类型
4. 编写清晰的变更描述

## ❓ 有问题？

- 提出问题讨论
- 查看现有的 issue 和 PR
- 查阅文档

## 🙏 感谢！

您的贡献让这个项目对每个人来说都变得更好。感谢您花时间贡献！
